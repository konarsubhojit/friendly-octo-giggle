"use client";

// Re-exported from their own files — kept here for backwards compatibility
export { UserMenu } from "@/components/ui/UserMenu";
export { ProtectedRoute } from "@/components/ui/ProtectedRoute";
