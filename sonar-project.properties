sonar.projectKey=konarsubhojit_friendly-octo-giggle
sonar.organization=konarsubhojit

# `src/proxy.ts` is listed explicitly: it is the edge security perimeter
# (rate limiting, HTTPS enforcement, CSP, admin gate) and must be scanned.
sonar.sources=src/app,src/lib,src/components,src/contexts,src/features,src/hooks,src/proxy.ts
sonar.tests=__tests__
sonar.test.inclusions=__tests__/**/*.test.ts,__tests__/**/*.test.tsx

sonar.javascript.lcov.reportPaths=coverage/lcov.info
