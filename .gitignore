# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/
next-env.d.ts

# Production
/build
/dist

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env
.env*.dev*
.env *
# Self-hosted deployment secrets. `deploy/.env.selfhost.example` stays tracked
# as the template; the filled-in copy beside it must never be committed.
deploy/.env.selfhost

# Vercel
.vercel

# TypeScript
*.tsbuildinfo

/lib/generated/prisma

# Storage migration script resumability checkpoint (local run state, not source)
.storage-migration-checkpoint.json

# Documentation - only keep README.md and CONTRIBUTING.md in root, all other docs in /docs folder
/*.md
!README.md
!CONTRIBUTING.md
test-db-connection.ts

# Claude agent
tmpclaude*
nul

# Agent skill scratch output
.github/skills/**/.last-review.md
certificates/
playwright-tests/report/
playwright-tests/screenshots/
playwright-tests/results/
test-results/
playwright-tests/.auth/
.playwright-mcp/**.log
.contextstream/
vitest-report.json
.cursor/mcp.json
.vscode/mcp.json
.vscode/mcp.json
.mcp.json

# Sentry Config File
.env.sentry-build-plugin
scripts/sql/catalog-data.sql

# Local dev/debug logs and captured cookies (never commit — may contain
# session tokens); scratch space for local Playwright/dev-server iteration.
.devlogs/
